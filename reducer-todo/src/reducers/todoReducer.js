export const initialState = {
    item: 'Learn about reducers',
    completed: false,
    id: 1,
    todos: []
}

export const reducer = (state, action) => {
    switch (action.type) {
        default: 
        return state;
    }
}